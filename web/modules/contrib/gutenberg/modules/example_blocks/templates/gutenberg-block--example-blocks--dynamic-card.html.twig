{#
/**
 * @file
 * Default theme implementation to display an example Gutenberg block server-side.
 *
 * Available variables:
 * - block_name: The Gutenberg block name.
 * - block_content: The content of this block (generated by save function).
 * - block_attributes: The attributes of this block.
 * - attributes: array of HTML attributes populated by modules, intended to
 *   be added to the main container tag of this template.
 *
 * @see template_preprocess_gutenberg_block()
 * @see template_preprocess_gutenberg_block__example_blocks__dynamic_card()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes }}>
  <img src="{{ block_attributes.imageUrl }}" alt="{{ block_attributes.imageAlt }}" />
  <div>
    <h2>{{ block_attributes.title }}</h2>
    <p>{{ block_attributes.subhead }}</p>
  </div>
  <div class="dynamic-block-content">
    {{ block_content }}
  </div>
</div>
